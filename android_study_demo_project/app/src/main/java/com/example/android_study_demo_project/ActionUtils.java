package com.example.android_study_demo_project;

public interface ActionUtils {

    //the flag for register and send service,keep consistent(activity register)
    String ACTIONS_EQUES_UPDATE_IP = "com.derry.receiver_study_";

    //the flag for register and send service,keep consistent(static register)
    String ACTIONS_FLAG = "com.derry.receiver_flag_";
}
